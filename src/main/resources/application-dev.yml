# 개발 환경에 맞춤
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trendy_dev_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: trendy_dev
    password: pass123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
#logging:
#  level:
#    root: DEBUG
#  file:
#    path: var/logs/trendy-dev.log
logging:
  level:
    org:
      springframework:
        orm:
          jpa: debug



